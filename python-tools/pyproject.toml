[project]
name = "python-tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "superclaude>=4.0.8",
]

[tool.uv.workspace]
members = [
    "python-tools",
]
